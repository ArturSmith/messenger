<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Replace with desired background color -->
    <corners android:radius="50dp" /> <!-- Radius value controls the roundness of the oval shape -->
    <stroke
        android:width="2dp"
        android:color="@android:color/holo_orange_dark"/>
</shape>